# Docker image
Docker image to run git application.

## Installation

### Build image

```
docker build --rm -t aberwag/nvm .
```

### Run container

```
docker-compose -f nvm.yml up'
```

### License ISC
